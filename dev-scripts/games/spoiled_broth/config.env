# --- Which game directory (relative to repo root) ---
GAME_DIR="games/spoiled_broth"

# --- Engine version to pin in the container ---
ENGINE_VERSION="latest"

# ---- Game Configuration ---
GAME_CAPACITY=2

# --- Shard sizing ---
SHARD_COUNT=14
SHARD_CPU=1
SHARD_MEMORY=1Gi
SHARD_CONCURRENCY=40
SHARD_MIN_INSTANCES=0
SHARD_MAX_INSTANCES=1

# --- Lobby (optional; comment LOBBY_DIR out to skip) ---
LOBBY_CPU=1
LOBBY_MEMORY="256Mi"
LOBBY_CONCURRENCY=80
