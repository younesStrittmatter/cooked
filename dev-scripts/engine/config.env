# --- Engine build/deploy config ---

# Relative path (from repo root) to your engine source directory
ENGINE_DIR="engine"

# Distribution/package name (must match pyproject.toml / setup.cfg)
PKG_NAME="cooked-engine"

# Artifact Registry repo name for Python wheels
PY_REPO="py-packages"
